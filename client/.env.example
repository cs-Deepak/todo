# Backend API URL
# For local development, use: http://localhost:6005
# For production (Vercel), use your deployed backend URL
VITE_API_URL=http://localhost:6005

# For production builds, set VITE_API_URL to your backend URL in the Vercel project environment variables
# Example: VITE_API_URL=https://todo-backend-steel-six.vercel.app

# Note: Vite replace `import.meta.env.VITE_API_URL` at build time. If you deploy to Vercel, add a Vite env var in the Vercel dashboard and rebuild.
