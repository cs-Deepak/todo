CLIENT_URL=https://todo-frontend-your-deploy.vercel.app
JWT_SECRET=your_jwt_secret_here
DATABASE=mongodb+srv://<user>:<pass>@cluster0.mongodb.net/your-db?retryWrites=true&w=majority

# Notes:
# - Add a REAL `JWT_SECRET` to production or to your hosting platform's env settings (do NOT commit secret to repo)
# - Set `CLIENT_URL` to your frontend deployment (vercel, render, or localhost://5173)
# - Keep sensitive credentials out of the repository; this example is for reference only
